export type ProjectType = {
  title: string;
  shortDescription: string;
  image: string;
  video?: string;
  technologies: string[];
  features: string[];
  serviceType: string;
  githubUrl: string;
};